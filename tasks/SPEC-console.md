# Console design

Objective: a focused management console with persistent sidebar, readable names,
accessible calendar selection, coherent colors and radii, and clear review actions.
Reuse Base UI/shadcn components and the existing organization heatmap. No fabricated
analytics. App shell lives in apps/web; shared primitives/tokens in packages/ui.

Verify desktop/mobile in an isolated browser, keyboard navigation, no UUIDs in
project selectors, and clean console. Run Biome, types, and production build.
Preserve server-side owner authorization and separate the marketing layout.
